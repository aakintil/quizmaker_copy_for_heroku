<div class='roles_checkboxes'>
	<strong>Roles:</strong>
	<br />
	  <% for role in @roles  %>
	   <%= check_box_tag "user[role_ids][]", role.id, @user.roles.include?(role) %>
		 <%= "#{role.name}" %>
	<% end  %>
</div>