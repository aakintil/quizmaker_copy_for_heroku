<% title "Roles" %>

<table>
  <tr>
    <th>Role</th>
	<th></th>
	<th></th>
  </tr>
  <% for role in @roles %>
    <tr>
      <td><%= role.name %></td>
      <td><%= link_to image_tag("edit_dg.gif"), edit_role_path(role) %></td>
      <td><%= link_to image_tag("delete_dg.gif"), :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Role", new_role_path %></p>
